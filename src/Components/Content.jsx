export default function Content ({regionCode , countryCode , name})
{

    return(
        <>
        <button>{regionCode}</button>
        <button>{countryCode}</button>
        <button>{name} </button> <br /> 
        
        </>
    );

}
